# matlabUiHacks
MATLAB introduced the `uifigure()` in the 2016a release, though a lot of functionality, compared to the existing `figure`, was missing. In an attempt to extend, explore and share the undocumented ways in which we can modify the new "Web-GUI" framework, I have created this collection.
